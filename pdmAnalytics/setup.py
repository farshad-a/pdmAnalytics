from setuptools import setup

setup(name='pdmAnalytics',
      version='0.0.1',
      # And any other dependencies l4dc needs
      install_requires=['mysql-connector==2.2.9', 'paho-mqtt==1.5.0']
      )
